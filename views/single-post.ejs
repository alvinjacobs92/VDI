<!DOCTYPE html>
<html lang="en">
    <%- include('includes/head') %>
<body>
    <%- include('includes/header') %>

    <main>
        <h1><%= post.title %></h1>

        <article>
            <small>posted by <%= post.username %> on <%=new Date(post.createdDate).getDate()%>/<%= new Date(post.createdDate).getMonth()+1%>/<%=new Date(post.createdDate).getFullYear()%></small>
            <div>
                <%= post.body%>
            </div>
        </article>

        <form style="display: inline" action="/download-file/<%=post.body%>" >
            <button>Download File</button>
        </form>
        <form style="display: inline" action="/download-signature/<%=post.body%>">
            <button>Download Signature</button>
        </form>

    </main>
    <%- include('includes/footer') %>

    
</body>
</html>